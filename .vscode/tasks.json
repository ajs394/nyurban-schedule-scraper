{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Launch Current File As Python",
            "command": "${workspaceFolder}/pythonpath.bat",
            "type": "shell",
            "args": [
                "${workspaceFolder}",
                "${file}"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}